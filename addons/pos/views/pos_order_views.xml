<?xml version="1.0" encoding="utf-8"?>
<ocean>
    <data>
        <!-- POS Order Tree View -->
        <record id="view_pos_order_tree" model="ocean.ui.view">
            <field name="name">pos.order.tree</field>
            <field name="model">pos.order</field>
            <field name="arch" type="xml">
                <tree string="POS Orders" decoration-muted="state=='cancel'" decoration-success="state=='done'" decoration-info="state=='paid'">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="session_id"/>
                    <field name="employee_id"/>
                    <field name="date_order"/>
                    <field name="amount_total"/>
                    <field name="sales_commission"/>
                    <field name="state"/>
                    <field name="user_id"/>
                </tree>
            </field>
        </record>

        <!-- POS Order Form View -->
        <record id="view_pos_order_form" model="ocean.ui.view">
            <field name="name">pos.order.form</field>
            <field name="model">pos.order</field>
            <field name="arch" type="xml">
                <form string="POS Order">
                    <header>
                        <button name="action_confirm" string="Confirm" type="object" class="btn-primary" states="draft"/>
                        <button name="action_pay" string="Pay" type="object" class="btn-success" states="confirmed"/>
                        <button name="action_done" string="Done" type="object" class="btn-primary" states="paid"/>
                        <button name="action_cancel" string="Cancel" type="object" class="btn-secondary" states="draft,confirmed"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,paid,done,cancel"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" placeholder="Order Reference"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="partner_id">
                                    <tree>
                                        <field name="name"/>
                                        <field name="email"/>
                                        <field name="phone"/>
                                    </tree>
                                </field>
                                <button name="%(action_pos_customer_wizard)d" string="Create New Customer" type="action" class="btn-primary" context="{'default_partner_id': partner_id}"/>
                                <field name="session_id"/>
                                <field name="user_id"/>
                                <field name="employee_id"/>
                                <field name="date_order"/>
                                <field name="order_type"/>
                            </group>
                            <group>
                                <field name="amount_untaxed"/>
                                <field name="amount_tax"/>
                                <field name="amount_total"/>
                                <field name="amount_paid"/>
                                <field name="amount_return"/>
                                <field name="sales_commission"/>
                                <field name="commission_rate"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Order Lines" name="order_lines">
                                <field name="order_line_ids">
                                    <tree editable="bottom">
                                        <field name="product_id"/>
                                        <field name="product_uom_qty"/>
                                        <field name="price_unit"/>
                                        <field name="discount"/>
                                        <field name="price_subtotal"/>
                                        <field name="price_subtotal_incl"/>
                                    </tree>
                                </field>
                            </page>
                            <page string="Payments" name="payments">
                                <field name="payment_ids">
                                    <tree editable="false">
                                        <field name="name"/>
                                        <field name="payment_method_id"/>
                                        <field name="amount"/>
                                        <field name="state"/>
                                        <field name="payment_date"/>
                                    </tree>
                                </field>
                            </page>
                            <page string="Receipts" name="receipts">
                                <field name="receipt_ids">
                                    <tree editable="false">
                                        <field name="name"/>
                                        <field name="receipt_type"/>
                                        <field name="amount"/>
                                        <field name="state"/>
                                        <field name="print_date"/>
                                    </tree>
                                </field>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- POS Order Search View -->
        <record id="view_pos_order_search" model="ocean.ui.view">
            <field name="name">pos.order.search</field>
            <field name="model">pos.order</field>
            <field name="arch" type="xml">
                <search string="Search POS Orders">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="session_id"/>
                    <field name="user_id"/>
                    <field name="employee_id"/>
                    <separator/>
                    <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                    <filter string="Confirmed" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                    <filter string="Paid" name="paid" domain="[('state', '=', 'paid')]"/>
                    <filter string="Done" name="done" domain="[('state', '=', 'done')]"/>
                    <filter string="Cancelled" name="cancel" domain="[('state', '=', 'cancel')]"/>
                    <filter string="Today" name="today" domain="[('date_order', '>=', datetime.datetime.combine(context_today(), datetime.time(0,0,0))), ('date_order', '&lt;=', datetime.datetime.combine(context_today(), datetime.time(23,59,59)))]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter string="Partner" name="partner" context="{'group_by': 'partner_id'}"/>
                        <filter string="Session" name="session" context="{'group_by': 'session_id'}"/>
                        <filter string="User" name="user" context="{'group_by': 'user_id'}"/>
                        <filter string="Employee" name="employee" context="{'group_by': 'employee_id'}"/>
                        <filter string="State" name="state" context="{'group_by': 'state'}"/>
                        <filter string="Order Date" name="order_date" context="{'group_by': 'date_order:day'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- POS Order Action -->
        <record id="action_pos_order" model="ocean.actions.act_window">
            <field name="name">POS Orders</field>
            <field name="res_model">pos.order</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_pos_order_search"/>
            <field name="context">{'search_default_today': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new POS order
                </p>
                <p>
                    POS orders track sales transactions and payments for each customer purchase.
                </p>
            </field>
        </record>
    </data>
</ocean>