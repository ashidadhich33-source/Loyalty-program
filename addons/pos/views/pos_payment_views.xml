<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- POS Payment Tree View -->
        <record id="view_pos_payment_tree" model="ir.ui.view">
            <field name="name">pos.payment.tree</field>
            <field name="model">pos.payment</field>
            <field name="arch" type="xml">
                <tree string="POS Payments">
                    <field name="name"/>
                    <field name="order_id"/>
                    <field name="payment_method_id"/>
                    <field name="amount"/>
                    <field name="state"/>
                    <field name="payment_date"/>
                </tree>
            </field>
        </record>
        
        <!-- POS Payment Form View -->
        <record id="view_pos_payment_form" model="ir.ui.view">
            <field name="name">pos.payment.form</field>
            <field name="model">pos.payment</field>
            <field name="arch" type="xml">
                <form string="POS Payment">
                    <header>
                        <button name="action_confirm" 
                                string="Confirm" 
                                type="object" 
                                class="btn-primary"
                                attrs="{'invisible': [('state', '!=', 'draft')]}"/>
                        <button name="action_cancel" 
                                string="Cancel" 
                                type="object" 
                                class="btn-secondary"
                                attrs="{'invisible': [('state', '=', 'done')]}"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,done"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" placeholder="Payment Reference"/>
                            </h1>
                        </div>
                        
                        <group>
                            <group>
                                <field name="order_id"/>
                                <field name="session_id"/>
                                <field name="payment_method_id"/>
                                <field name="amount"/>
                                <field name="payment_date"/>
                            </group>
                            <group>
                                <field name="payment_reference"/>
                                <field name="digital_payment_id"/>
                                <field name="digital_payment_status"/>
                                <field name="cash_amount"/>
                                <field name="change_amount"/>
                            </group>
                        </group>
                        
                        <notebook>
                            <page string="Notes">
                                <field name="note" widget="text"/>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>
        
        <!-- POS Payment Action -->
        <record id="action_pos_payment" model="ir.actions.act_window">
            <field name="name">POS Payments</field>
            <field name="res_model">pos.payment</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_done': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No payments found!
                </p>
                <p>
                    Payments are automatically created when processing POS orders.
                </p>
            </field>
        </record>
        
    </data>
</odoo>