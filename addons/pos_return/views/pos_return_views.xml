<?xml version="1.0" encoding="utf-8"?>
<ocean>
    <data>
        <!-- POS Return Tree View -->
        <record id="view_pos_return_tree" model="ocean.ui.view">
            <field name="name">pos.return.tree</field>
            <field name="model">pos.return</field>
            <field name="arch" type="xml">
                <tree string="POS Returns" decoration-muted="state=='cancel'" decoration-success="state=='done'" decoration-info="state=='draft'">
                    <field name="name"/>
                    <field name="original_order_id"/>
                    <field name="customer_id"/>
                    <field name="return_date"/>
                    <field name="state"/>
                    <field name="total_amount"/>
                    <field name="return_type"/>
                </tree>
            </field>
        </record>

        <!-- POS Return Form View -->
        <record id="view_pos_return_form" model="ocean.ui.view">
            <field name="name">pos.return.form</field>
            <field name="model">pos.return</field>
            <field name="arch" type="xml">
                <form string="POS Return">
                    <header>
                        <button name="action_confirm" string="Confirm" type="object" class="btn-primary" states="draft"/>
                        <button name="action_process" string="Process" type="object" class="btn-success" states="confirmed"/>
                        <button name="action_done" string="Done" type="object" class="btn-primary" states="processed"/>
                        <button name="action_cancel" string="Cancel" type="object" class="btn-secondary" states="draft,confirmed"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,processed,done,cancel"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" placeholder="Return Reference"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="original_order_id"/>
                                <field name="customer_id"/>
                                <field name="return_date"/>
                                <field name="return_type"/>
                                <field name="user_id"/>
                            </group>
                            <group>
                                <field name="total_amount"/>
                                <field name="currency_id"/>
                                <field name="return_fee"/>
                                <field name="refund_amount"/>
                                <field name="restocking_fee"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Return Lines" name="return_lines">
                                <field name="return_line_ids">
                                    <tree editable="bottom">
                                        <field name="product_id"/>
                                        <field name="quantity"/>
                                        <field name="unit_price"/>
                                        <field name="total_price"/>
                                        <field name="return_reason_id"/>
                                        <field name="notes"/>
                                    </tree>
                                </field>
                            </page>
                            <page string="Kids Clothing Specific" name="kids_clothing">
                                <group>
                                    <group>
                                        <field name="age_group"/>
                                        <field name="season"/>
                                        <field name="size_range"/>
                                        <field name="gender"/>
                                    </group>
                                    <group>
                                        <field name="special_occasion"/>
                                        <field name="gift_wrapping"/>
                                        <field name="gift_message"/>
                                        <field name="care_instructions"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- POS Return Search View -->
        <record id="view_pos_return_search" model="ocean.ui.view">
            <field name="name">pos.return.search</field>
            <field name="model">pos.return</field>
            <field name="arch" type="xml">
                <search string="Search POS Returns">
                    <field name="name"/>
                    <field name="original_order_id"/>
                    <field name="customer_id"/>
                    <field name="user_id"/>
                    <separator/>
                    <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]"/>
                    <filter string="Confirmed" name="confirmed" domain="[('state', '=', 'confirmed')]"/>
                    <filter string="Processed" name="processed" domain="[('state', '=', 'processed')]"/>
                    <filter string="Done" name="done" domain="[('state', '=', 'done')]"/>
                    <filter string="Cancelled" name="cancel" domain="[('state', '=', 'cancel')]"/>
                    <filter string="Today" name="today" domain="[('return_date', '>=', datetime.datetime.combine(context_today(), datetime.time(0,0,0))), ('return_date', '&lt;=', datetime.datetime.combine(context_today(), datetime.time(23,59,59)))]"/>
                    <separator/>
                    <group expand="0" string="Group By">
                        <filter string="Customer" name="customer" context="{'group_by': 'customer_id'}"/>
                        <filter string="Original Order" name="original_order" context="{'group_by': 'original_order_id'}"/>
                        <filter string="Return Type" name="return_type" context="{'group_by': 'return_type'}"/>
                        <filter string="State" name="state" context="{'group_by': 'state'}"/>
                        <filter string="Return Date" name="return_date" context="{'group_by': 'return_date:day'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- POS Return Action -->
        <record id="action_pos_return" model="ocean.actions.act_window">
            <field name="name">POS Returns</field>
            <field name="res_model">pos.return</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_pos_return_search"/>
            <field name="context">{'search_default_today': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a new POS return
                </p>
                <p>
                    POS returns track product returns for customer orders.
                </p>
            </field>
        </record>
    </data>
</ocean>